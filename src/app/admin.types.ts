export interface SlaPolicy {
  id: string | number;
  policyName: string;
}

export interface Department {
  id: string | number;
  name: string;
}

export interface Role {
  id: string | number;
  name: string;
}